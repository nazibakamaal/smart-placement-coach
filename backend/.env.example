PORT=5000
# MongoDB Atlas Free Tier — replace placeholders with your cluster credentials
# Cluster > Connect > Drivers > copy connection string, then set database name
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/smart-placement-coach?retryWrites=true&w=majority
JWT_SECRET=supersecretjwtkeyforplacementcoach
GEMINI_API_KEY=
